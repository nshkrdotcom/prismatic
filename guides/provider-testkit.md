# Provider Testkit

`prismatic_provider_testkit` is the shared verification package for provider
repos.

It exists to keep freshness checks, conformance rules, and artifact validation
generic rather than reimplemented in every provider repo.
