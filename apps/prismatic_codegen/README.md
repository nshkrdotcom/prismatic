# Prismatic Codegen

Shared compiler and rendering package for `prismatic` provider SDKs.

This package owns:

- provider definitions
- GraphQL-native provider IR
- compiler transforms
- minimal renderer helpers
