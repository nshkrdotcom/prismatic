# Examples

The root workspace does not ship runnable provider examples itself.

Examples belong in provider repos, while the shared workspace focuses on
runtime, compiler, and verification behavior.
